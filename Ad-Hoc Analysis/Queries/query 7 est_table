SELECT s.*, f.forecast_quantity
 FROM fact_sales_monthly s
LEFT JOIN fact_forecast_monthly f
 USING (date, cust_id, prod_id)

UNION ALL

SELECT s.*, f.forecast_quantity
 FROM fact_sales_monthly s
RIGHT JOIN fact_forecast_monthly f
 USING (date, cust_id, prod_id)
WHERE s.date IS NULL 
